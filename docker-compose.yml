services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_URL: sqlite:///./data/darkweb_monitoring.sqlite3

